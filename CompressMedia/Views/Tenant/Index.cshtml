@model IEnumerable<CompressMedia.DTOs.TenantDto>

@{
	ViewBag.Title = "Tenants";
	Layout = "_Layout";
}

<h2>Roles</h2>
<button type="button" class="btn btn-primary">
	<a asp-controller="Tenant" asp-action="CreateTenant" style="text-decoration: none; color: white;"> Add New Tenant</a>
</button>
<table class="table table-bordered mt-4">
	<thead>
		<tr>
			<th>Name</th>
			<th>Action</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var tenant in Model)
		{
			<tr>
				<th>@tenant.TenantName</th>
				<td>
					<a asp-controller="Tenant" asp-action="ViewBlob" asp-route-blobId="@tenant.TenantId" class="btn btn-outline-info">Edit</a>
					<a asp-controller="Tenant" asp-action="DeleteBlob" asp-route-blobId="@tenant.TenantId" class="btn btn-outline-danger">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>

<script>
	setTimeout(function () {
		location.reload();
	}, 10000);
</script>